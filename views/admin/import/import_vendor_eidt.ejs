<!-- import_vendor_edit.ejs -->
<form method="POST" action="/admin/import/edit/<%= vendor.id %>" class="form">
    <!-- input 들은 위와 동일하게 배치 -->
    <input type="date" name="date" value="<%= vendor.date.toISOString().split('T')[0] %>" required>
    <input type="text" name="v_name" value="<%= vendor.v_name %>" required>
    ...
    <button type="submit">✏️ 저장</button>
  </form>
  